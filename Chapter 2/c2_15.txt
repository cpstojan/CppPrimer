Which of the following definitions, if any, are invalid? Why?
(a) int ival = 1.01; // Valid
(b) int &rval1 = 1.01; // Invalid, reference can't be to literal
(c) int &rval2 = ival; // Valid
(d) int &rval3; // Invalid, reference can't be uninitialized
